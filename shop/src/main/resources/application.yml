# ?????? ?? ??
server:
  port: 80

# MySQL ??
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shop?serverTimezone=UTC
    username: root
    password:
  # 파일 한 개당 최대 사이즈
  servlet:
    multipart:
      max-file-size: 20MB
  # ???? ?? ?? ??
  jpa:
    # show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL8Dialect
# Live Reload Active
  devtools:
    livereload:
      enabled: true
  # Thymeleaf cache false
  thymeleaf:
    cache: false

# ??? ???? ???? ??? ???? ??
logging:
  level:
    sql: trace


# 상품 이미지 업로드 경로
itemImgLocation: /Users/yeonnex/Desktop
# 리소스 업로드 경로
uploadPath: /Users/yeonnex/Desktop