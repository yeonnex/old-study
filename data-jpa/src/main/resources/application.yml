spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/datajpa
    driver-class-name: org.h2.Driver
    username: sa
    password: d

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true
logging:
  level:
    org.hibernate.sql: debug
